# 模型下载使用示例

## 1. 查看帮助信息

```bash
node download_model.js --help
```

输出：
```
🚀 模型下载工具
==================================================
用法: node download_model.js [选项] 

选项:
  --model, -m <model>     指定要下载的模型 (默认: Xenova/whisper-tiny)  
  --quantized, -q [true/false]  是否下载量化模型 (默认: false)
  --help, -h              显示帮助信息

支持的模型:
  1. Xenova/whisper-tiny
     最小最快，精度最低
     大小: ~75MB, 精度: 低
  2. Xenova/whisper-base
     基础模型，平衡速度和精度       
     大小: ~142MB, 精度: 中低       
  3. Xenova/whisper-small
     小模型，较好的精度和速度       
     大小: ~466MB, 精度: 中
  4. Xenova/whisper-medium
     中等模型，高精度
     大小: ~1.5GB, 精度: 高
  5. Xenova/whisper-large
     大模型，最高精度，速度较慢     
     大小: ~2.9GB, 精度: 最高       
  6. distil-whisper/distil-whisper-tiny
     蒸馏小模型，更快
     大小: ~61MB, 精度: 低
  7. distil-whisper/distil-whisper-base
     蒸馏基础模型
     大小: ~125MB, 精度: 中低       
  8. distil-whisper/distil-whisper-small
     蒸馏小模型
     大小: ~401MB, 精度: 中
  9. distil-whisper/distil-whisper-medium
     蒸馏中等模型
     大小: ~1.2GB, 精度: 高

示例:
  node download_model.js --model Xenova/whisper-small
  node download_model.js -m Xenova/whisper-medium --quantized true      
```

## 2. 下载默认模型（whisper-tiny）

```bash
node download_model.js
```

## 3. 下载指定精度的模型

### 下载基础模型
```bash
node download_model.js --model Xenova/whisper-base
```

### 下载小模型
```bash
node download_model.js -m Xenova/whisper-small
```

### 下载中等模型
```bash
node download_model.js --model Xenova/whisper-medium
```

### 下载大模型（最高精度）
```bash
node download_model.js -m Xenova/whisper-large
```

## 4. 下载量化模型

### 下载量化的小模型
```bash
node download_model.js --model Xenova/whisper-small --quantized true
```

### 下载量化的中等模型
```bash
node download_model.js -m Xenova/whisper-medium -q
```

## 5. 下载蒸馏模型

### 下载蒸馏小模型
```bash
node download_model.js --model distil-whisper/distil-whisper-small
```

### 下载量化的蒸馏中等模型
```bash
node download_model.js -m distil-whisper/distil-whisper-medium --quantized true
```

## 6. 模型选择建议

| 使用场景 | 推荐模型 | 理由 |
|---------|---------|------|
| 开发测试 | whisper-tiny | 快速验证功能，下载速度快 |
| 实时语音转写 | whisper-small 或 distil-whisper-small | 平衡速度和精度 |
| 高精度转录 | whisper-medium 或 distil-whisper-medium | 高精度，适合重要文档转录 |
| 最高精度需求 | whisper-large | 适合对精度要求极高的场景 |
| 资源受限环境 | 量化版本的模型 | 更小的体积和内存占用 |

## 7. 模型下载状态查看

下载过程中会显示详细的进度信息，包括：
- 尝试次数和使用的下载源
- 下载配置信息
- 每个文件的下载进度
- 下载速度
- 下载完成后的模型测试结果
- 下载的文件列表

## 8. 后续使用

下载完成后，可以使用以下命令测试和使用模型：

### 测试模型
```bash
node test.js your-audio-file.wav
```

### 启动服务器
```bash
npm start
```

### 或使用示例脚本
```bash
node examples.js
```

## 9. 离线使用

模型下载完成后会缓存到本地，后续使用无需网络连接，可以离线运行。模型默认存储在项目的 `models` 目录下。